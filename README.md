# emotion_tracker
## Тут что-то будет

## Фронтенд

Статическая страница лежит в `app/templates/index.html`. Запуск через Docker:

```bash
docker compose build
docker compose up -d
```

В браузере: [http://localhost:8090](http://localhost:8090) (порт задаётся в `docker-compose.yml` в `ports`).
